import React from "react";
import styled from "styled-components";

const PrimaryHeaderView = styled.div`
    display: flex;
    flex: 0.025;
    height: 2.5vh;
    background-color: yellow;
`
export const PrimaryHeader = ({}) => {
    return (
        <PrimaryHeaderView>PrimaryHeaderView</PrimaryHeaderView>
    )
}